---
description: Initialize Moderails protocol
---

**Project**: `{{ project_root }}`

Run from the project directory:

```sh
moderails start
```

If the user included flags (e.g. `/moderails --rerail`), append them: `moderails start --rerail`
